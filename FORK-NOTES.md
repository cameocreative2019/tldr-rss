# Fork deployment notes

This fork publishes expanded TLDR feeds through GitHub Pages for use by CondenseIt.

- Workflow: `.github/workflows/deploy.yml`
- Schedule: daily at 12:00 UTC
- Manual runs: Actions → Update RSS → Run workflow

The generated feeds are public; no API keys or newsletter credentials are stored here.
